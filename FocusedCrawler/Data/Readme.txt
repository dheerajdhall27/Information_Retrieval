The program could be run on pycharm, the version of python being used is "3.7.4"

1)Unzip the contents of the package DheerajKumar_Dhall_HW1 and place it in your local system.

2)Libraries needed to make the program run
pip install beautifulsoup4
pip install lxml
pip install requests
pip install uuid
pip install urllib3

3)Execute the crawler.py class by navigating to the folder and running on terminal by calling (python crawler.py) from terminal

4) The crawled urls will be written to a file in the same directory with the name FocusedData.txt

The total number of duplicates found = 29
The retreival percentage is 2.9%
